# Example configuration for webrtc-ipcam server
# Lines starting with # are comments

addr = 8765
width = 1280
height = 720
framerate = 30
rotation = 180
cors_origin = "*"

# Optional: Bitrate limiting in bits/sec (e.g., 1000000 = 1Mbps)
# Highly recommended for Pi Zero 2 W to prevent encoder overload
# Pi Zero 2 W: try 1000000 (1Mbps) or 1500000 (1.5Mbps)
# Pi 4: 2000000-4000000 (2-4Mbps) works well
# bitrate = 1500000

# Optional: uncomment to enable recording (directory must exist and be writable)
# recording_dir = /mnt/external/recordings
